spring:
  datasource:
    url: jdbc:mysql://localhost:3311/hospital_management_system
    username: adem
    password: adem1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
